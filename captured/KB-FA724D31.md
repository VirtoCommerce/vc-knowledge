---
id: KB-FA724D31
subject: storefront Delete member detaches the contact and orphans the account
plane: experiential
question: What does deleting a member from the storefront company members list actually delete?
status: active
refutableBy: observation
appliesTo:
  - axis: surface
    value: storefront-ui
  - axis: surface
    value: admin-ui
anchors:
  - coordinate: Mutations.removeMemberFromOrganization
evidence:
  - method: observation
    deployment: vcptcore_stable
    at: 2026-09-11T18:02:54.798Z
    by: session:8df1fb2c
  - method: observation
    deployment: vcptcore_stable
    at: 2026-09-12T08:22:26.788Z
    by: session:f00f5968
  - method: observation
    deployment: vcptcore_stable
    at: 2026-09-12T08:35:49.719Z
    contradicts: true
    note: Everything this entry says about what Delete does held exactly and I confirmed it. The remedy in its last sentence did not. After an un-accepted invitation was walked through Edit role, Block, Unblock and Delete, the surviving account could not be deleted under Security > Users - it is absent from POST /api/platform/security/users/search by keyword, by memberId and unfiltered, so the grid cannot show it at all, and DELETE /api/platform/security/users?names= fails with ConcurrencyFailure on every attempt. Control accounts in the same store behave normally. So 'to actually delete someone, delete the account under Security > Users' is not always available; see KB-4A8606CA.
---

It deletes nothing. The storefront's Delete member action removes the organization membership and nothing else: the row leaves the roster, but in Admin the contact still exists with its status unchanged, its security account still exists, is still Locked or Unlocked exactly as before, and STILL HOLDS the organization role it was given - an org-scoped role now assigned to an account that belongs to no organization. On the contact blade the only thing that changed is that 'Member of company(ies)' and 'Default company' are empty. Two consequences. First, removal-from-organization is invisible from the storefront, because the only representation of it is the row's absence; there is no removed state to display and no way to see a former member. Second, the email address stays taken by the surviving account, so re-inviting the same address later is not the clean slate it looks like. To actually delete someone, delete the account under Security > Users and the contact under Contacts; the storefront cannot do either, which also means a maintainer cannot create the deleted state at all - only the detached one.

**Disputed.** Everything this entry says about what Delete does held exactly and I confirmed it. The remedy in its last sentence did not. After an un-accepted invitation was walked through Edit role, Block, Unblock and Delete, the surviving account could not be deleted under Security > Users - it is absent from POST /api/platform/security/users/search by keyword, by memberId and unfiltered, so the grid cannot show it at all, and DELETE /api/platform/security/users?names= fails with ConcurrencyFailure on every attempt. Control accounts in the same store behave normally. So 'to actually delete someone, delete the account under Security > Users' is not always available; see KB-4A8606CA. — observed on `vcptcore_stable`.

**Anchor corrected.** `Mutations.deleteOrganizationContact` → `Mutations.removeMemberFromOrganization` — no deleteOrganizationContact exists in the schema or in vc-frontend; the storefront Delete member action calls removeMemberFromOrganization (client-app/core/api/graphql/organization/mutations/removeMemberFromOrganization), which is what this entry observed doing nothing but detach
